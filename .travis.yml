branches:
  only:
  - develop

matrix:
  include:
    - language: python
      python:
        - 3.7
      before_install:
        - cd backend/
      install:
        - pip install -r requirements.txt
      before_script:
        - cd app/
      script:
        - pytest && flake8
#    - language: node_js
#      node_js:
#        - 10.16
#      before_install:
#        - cd frontend/
#      install:
#        - yarn install
#      script:
#        - yarn test

notifications:
  email: false